Everything is built in the editor. To build a map, simply add the MapController script to any object, add the 2 scriptable objects required and press build map.
Or use the existing map object which already has everything set up
